[pytest]
; Make the notes_api package importable no matter how pytest is invoked,
; "pytest tests/" in CI does not add the working directory to sys.path
; the way "python -m pytest" does.
pythonpath = .
testpaths = tests
